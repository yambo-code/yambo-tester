# Empty parameter means default
parameters:
    label: cirun
    tests_dir:
    scratch_dir: scratch
    cache_dir: cache
    yambo_bin:
    yambo: yambo
    ypp: ypp
    p2y: p2y
    mpi_launcher: mpirun
    nprocs: 2
    tollerance: 0.1
tests:
    Al_bulk: ["GW-OPTICS"]
    #Si_bulk: ["GW-OPTICS"]
    #hBN: ["GW-OPTICS"]
    #LiF: ["GW-OPTICS"]
